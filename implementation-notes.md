# Implementation notes

## Deviations

- 2026-08-18 (Opus 5 migration fixes): plan assumed the claude-opus-5 / max_tokens 16000 changes were already committed; territory: they existed only as uncommitted edits in the main checkout's working tree (branch `main`), not in this worktree. Ported the `packages/` portion of that diff into this branch (left the unrelated `.gitignore` `.serena/` hunk behind) so the fixes layer on top of the migration.
- 2026-08-18 (structured outputs): plan assumed the json_schema could be derived directly from `JobAnalysisSchema` (zod v3); territory: `@anthropic-ai/sdk` ≥0.117 `zodOutputFormat` is typed against `zod/v4` only. Kept models on classic zod v3 and mirrored the response shape with `zod/v4` inside `job-analysis-agent.ts`; canonical `JobAnalysisSchema.parse` still runs on the final object so drift between the two fails loudly at runtime.
- 2026-08-18 (cli args): plan assumed fixing the package.json script (`--headless` → `generate`) was sufficient; territory: pnpm (unlike npm) forwards the literal `--` separator into the script's argv, and commander then treats every flag after it as positional — `--job` was silently dropped. Added a bare-`--` strip in `src/cli/index.ts` before `program.parse()`.
- 2026-08-18 (audit fix): plan said the audit step in ci.yml was also failing; territory: ci.yml has no audit step — the only `pnpm audit` gate is security-scan.yml's `--audit-level=high`. Fixed against that gate only.
- 2026-08-18 (audit fix): plan framed all 19 vulns as transitives needing root overrides; territory: multer, sharp, js-yaml, and file-type are direct deps of workspace packages. Took the conservative path per case — in-range lockfile updates (multer, js-yaml, file-type), a range bump for sharp (`^0.34.5` → `^0.35.0`, out-of-range fix), overrides only for true transitives — plus belt-and-braces override floors for multer/js-yaml. The js-yaml override is scoped `js-yaml@>=4.0.0` because a js-yaml@3.14.2 also lives in the tree and a blanket floor would force it across the 3→4 breaking major.
- 2026-08-18 (audit fix): plan implied clearing all 19 vulns; territory: uuid GHSA-w5hq-g745-h8pq (moderate) needs a breaking 10→11 major jump for one tree instance. Left it (moderate is below the CI gate) and documented it as accepted in the overrides comment.
- 2026-09-24 (audit fix refresh): plan assumed PR #152's green audit (2026-08-19) still held at merge time; territory: 5 weeks of new advisories re-broke the gate on the PR branch itself (16 high — @xmldom/xmldom ≤0.9.11, multer <2.3.0, sharp <0.35.4, js-yaml <4.3.2). Raised the same floors rather than opening a new PR; capped the new `uuid@>=13.0.0` override `<14.0.0` after an unbounded floor resolved to a 14.x major.
- 2026-09-24 (self-report reconciliation, #154): the brief assumed TD-001 (lockfile drift) was still open and asked for it as its own slice; it was resolved in PR #100 (merged 2026-02-28, pre-commit guard `scripts/check-lockfile.sh`). Left it out of the roadmap and kept the slice ids contiguous.
- 2026-09-24 (self-report reconciliation, #154): the brief treated "P1 keyless E2E" as one slice; the public bundle at cv.jim.software hard-codes `http://localhost:3001/api` and there's no API host, so a keyless E2E has nothing to run against. Split it into S4 (hosted API, `human_only`: spends the operator's key and touches deploy) and S5 (E2E + recording).
- 2026-09-24 (self-report reconciliation, #154): the brief said "V2 is the default" was false because the server gates V2 behind `ENABLE_V2_API`; the root `pnpm dev:*` scripts set the flag, so it's true for local dev and false for Docker, CI compose and a plain `start`. Documented it that way instead of calling the claim false outright.
- 2026-09-24 (self-report reconciliation, #154): the brief said `client-v2.ts:253` checks a `type` the server never sends; the mismatch is broader. The client parses only `data:`-first segments, and the server sends named `event:` frames, so frames are dropped depending on network chunking. Also found: `GET /api/v2/threads/:id` is called by `threadsSlice` but has no route. Both went into S1's scope; no code fixed here.
- 2026-09-24 (self-report reconciliation, #154): the brief offered "As built section or rewrite" for ARCHITECTURE_V2. I added an As built section and kept the 2025-12-04 design below a labeled "not as built" heading. The fabricated perf table, the feature-parity table and the RAG troubleshooting entry are deleted outright, because each read as fact about something that doesn't exist.

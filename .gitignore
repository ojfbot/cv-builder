# Environment variables
.env
.env.local
.env.*.local
env.json

# Private data directories
/bio/
/jobs/
/output/
/research/
/pipelines/
/toolbox/
personal/

# Temporary working directory
# See temp/.gitignore for what's tracked vs ignored within temp/
# Note: temp/ has its own .gitignore to selectively track PR screenshots
packages/browser-automation/temp/
# Monorepo root temp/ directory has selective tracking via temp/.gitignore
# (Do not add "temp/" here - it would override temp/.gitignore)
# Prevent nested package directory errors (e.g., packages/*/packages/*/)
packages/*/packages/

# Dependencies
node_modules/
/.pnp
.pnp.js

# pnpm
.pnpm-store/
.pnpm-debug.log
pnpm-lock.yaml

# npm (legacy - to be removed after migration)
package-lock.json
npm-debug.log*

# Testing
/coverage

# Production builds (NEVER commit build artifacts)
/build
/dist
build/
dist/
*.tsbuildinfo

# Misc
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.claude/settings.local.json

# Temporary files
*.tmp
.cache/

# SQLite databases (agent-graph)
*.db
*.db-wal
*.db-shm

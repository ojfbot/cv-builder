# IMPORTANT: Copy this file to .env.local (to avoid conflicts with Claude CLI)
# cp .env.example .env.local

# Anthropic API Key
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Vite-compatible API key (for browser usage - NOT RECOMMENDED FOR PRODUCTION)
# WARNING: This exposes your API key in the browser. Use a backend server in production.
VITE_ANTHROPIC_API_KEY=your_api_key_here

# GitHub API Authentication (for screenshot uploads and automation)
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo (full control of private repositories)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxx
# Alternative: GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxx

# Repository info (auto-detected from git config if not set)
# GITHUB_OWNER=ojfbot
# GITHUB_REPO=cv-builder

# Optional: Override default directories
# BIO_DIR=./bio
# JOBS_DIR=./jobs
# OUTPUT_DIR=./output
# PUBLIC_DIR=./public
